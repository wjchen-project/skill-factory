# AGENTS.md

## 项目说明

本项目为 **skill-factory**（技能工厂），专门用于创建和管理各类技能（skills）。技能是面向特定任务的标准化指令集合，能够为 Agent 提供专业化的处理流程与执行规范。

## 目录结构

- `skills/` — 技能源文件存放目录，所有新建技能均放置于此
- `.opencode/skills/` — 技能的副本目录，需与 `skills/` 目录保持同步

每次创建或更新技能时，必须**同时**写入 `skills/` 与 `.opencode/skills/` 两个目录，确保两边内容一致。

## 创建技能流程

每当用户提出创建技能的需求时，必须先向用户确认以下信息：

1. **技能用途（purpose）**
   - 该技能要解决什么问题？
   - 适用于哪些具体场景？
   - 输入与输出分别是什么？

2. **触发时机（trigger）**
   - 在何种情况下应当激活该技能？
   - 用户通常会用什么样的关键词或意图触发它？
   - 是否需要自动触发，还是需要用户显式调用？

在确认上述信息后，再根据需求判断是否需要为技能添加：

- **命令（commands）**：当技能需要提供可执行的快捷指令时添加
- **脚本（scripts）**：当技能涉及较复杂逻辑、文件处理或外部工具调用时添加
- **资源文件（references / assets）**：当技能需要附带示例数据、模板或文档时添加

## 技能编写规范

- 每个技能作为独立子目录存放，目录名采用小写加连字符命名（如 `code-reviewer/`）
- 技能主文件命名为 `SKILL.md`，置于技能目录根下
- `SKILL.md` 需包含清晰的技能说明、触发条件与执行步骤
- 保持 `skills/` 与 `.opencode/skills/` 内容一致