# AGENTS.md

## 项目说明

本项目为 **skill-factory**（技能工厂），专门用于创建和管理各类技能（skills）。技能是面向特定任务的标准化指令集合，能够为 Agent 提供专业化的处理流程与执行规范。

## 目录结构

- `skills/` — 技能源文件存放目录，所有新建技能均放置于此
- `.opencode/skills/` — 技能的引用目录，通过符号链接指向 `skills/` 中对应技能，避免重复维护

每次创建或更新技能时，只需在 `skills/` 下创建/修改，然后在该目录下执行 `ln -s ../../skills/<skill-name> <skill-name>` 让 `.opencode/skills/` 指向源目录。

## 创建技能流程

每当用户提出创建技能的需求时，**必须向用户至少提出 3 个澄清问题**，覆盖以下三个维度，确保需求明确后再开始编写：

### 1. 技能用途（purpose）
- 该技能要解决什么问题？
- 适用于哪些具体场景？
- 输入与输出分别是什么？

### 2. 触发时机（trigger）
- 在何种情况下应当激活该技能？
- 用户通常会用什么样的关键词或意图触发它？
- 是否需要自动触发，还是需要用户显式调用？

### 3. 交付与约束（deliverables & constraints）
- 技能是否需要附带 `commands`（可执行快捷指令）、`scripts`（处理脚本）或 `references / assets`（示例数据、模板、文档）？
- 是否存在必须遵循的语言、框架、命名或风格约束？
- 技能目录名是否已有偏好（如 `code-reviewer/`）？

> 上述三个维度至少各问一个问题；若任一维度的回答仍模糊，需继续追问，直到所有关键信息清晰为止。

在确认上述信息后，再根据需求决定是否需要为技能添加：

- **命令（commands）**：当技能需要提供可执行的快捷指令时添加
- **脚本（scripts）**：当技能涉及较复杂逻辑、文件处理或外部工具调用时添加
- **资源文件（references / assets）**：当技能需要附带示例数据、模板或文档时添加

## 技能编写规范

- 每个技能作为独立子目录存放，目录名采用小写加连字符命名（如 `code-reviewer/`）
- 技能主文件命名为 `SKILL.md`，置于技能目录根下
- `SKILL.md` 需包含清晰的技能说明、触发条件与执行步骤
- 通过符号链接保证 `skills/` 与 `.opencode/skills/` 内容一致
